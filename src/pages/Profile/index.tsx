import React from 'react';
import {RegisterForm} from "src/components/Forms";

const Profile = () => {
    return (
        <div>
            <RegisterForm />
        </div>
    );
};

export default Profile;